<!-- <h3 class="mt-3">
  Results for "<%= q %>"
</h3>

<% if (listings.length === 0) { %>
  <p>No results found</p>
<% } %>

<div class="row">
  <% listings.forEach(listing => { %>
    <div class="col-md-4">
      <div 
        class="card mb-3 listing-card"
        onclick="window.location='/listings/<%= listing._id %>'"
        style="cursor:pointer"
      >
        <img src="<%= listing.image %>" class="card-img-top">
        <div class="card-body">
          <h5><%= listing.title %></h5>
          <p><%= listing.location %>, <%= listing.country %></p>
        </div>
      </div>
    </div>
  <% }) %>
</div> -->
